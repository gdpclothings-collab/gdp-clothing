name: Apply Seasonal Approval Preview Patch

on:
  push:
    branches:
      - fix/custom-studio-canonical-flow-20260913

permissions:
  contents: write

jobs:
  apply:
    if: ${{ !contains(github.event.head_commit.message, '[seasonal-preview-applied]') }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v5
        with:
          ref: fix/custom-studio-canonical-flow-20260913
          fetch-depth: 0

      - name: Apply exact Seasonal approval preview patch
        run: node scripts/_patch_seasonal_approval_preview.mjs

      - name: Commit source patch
        run: |
          git config user.name "GDP Clothing Automation"
          git config user.email "actions@users.noreply.github.com"
          git add src/pages/CustomStudio.jsx
          git commit -m "fix(custom-studio): show exact Seasonal approval preview [seasonal-preview-applied]"
          git push origin HEAD:fix/custom-studio-canonical-flow-20260913
