name: Custom Studio Editor Redesign

on:
  push:
    branches:
      - custom-studio-editor-redesign-20260912

permissions:
  contents: write

jobs:
  apply-and-validate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: custom-studio-editor-redesign-20260912
          fetch-depth: 0

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Align migration guard
        run: |
          python - <<'PY'
          from pathlib import Path
          path = Path('scripts/apply-custom-studio-editor-redesign.mjs')
          source = path.read_text()
          needle = '  3,\n  "selected layer canvas emphasis"'
          replacement = '  2,\n  "selected layer canvas emphasis"'
          if needle not in source:
              raise SystemExit('selected-layer guard not found')
          path.write_text(source.replace(needle, replacement, 1))
          PY

      - name: Apply redesign
        run: node scripts/apply-custom-studio-editor-redesign.mjs

      - name: Build
        run: npm run build

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Commit generated source changes
        shell: bash
        run: |
          if git diff --quiet; then
            echo "No generated changes found."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/components/storefront/CustomStudioAdvancedEditor.jsx src/pages/CustomStudio.jsx
          git commit -m "feat: redesign custom studio editing workspace [skip ci]"
          git push origin HEAD:custom-studio-editor-redesign-20260912
