name: Apply Photo Bootleg multi-text hook-order fix

on:
  push:
    paths:
      - ".github/workflows/fix-photo-bootleg-multi-text-hook-order.yml"

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "22"

      - name: Apply hook-order fix
        run: node scripts/fix-photo-bootleg-multi-text-hook-order.mjs

      - name: Commit hook-order fix
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/components/storefront/custom-studio-v2/ProtectedTemplateEditorV2.jsx
          git commit -m "Fix Photo Bootleg multi-text hook order"
          git push
